# Etsy Open API v3 credentials
# Register at: https://www.etsy.com/developers/register
ETSY_API_KEY=your_keystring_here
ETSY_SHARED_SECRET=your_shared_secret_here

# Your Etsy shop ID (numeric, found in shop URL or API response)
ETSY_SHOP_ID=your_shop_id_here

# OAuth 2.0 redirect URI — local only, no server needed
REDIRECT_URI=http://localhost:8080/callback

# Token storage (auto-generated after first auth)
TOKEN_FILE=.etsy_token.json
